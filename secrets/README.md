# secrets/

Place service account JSON files here (e.g. `sheets-sa.json`).

**NEVER commit.** All files in this folder are gitignored except `.gitkeep` and this README.

Paths to these files are referenced from `.env` (see `.env.example`).
